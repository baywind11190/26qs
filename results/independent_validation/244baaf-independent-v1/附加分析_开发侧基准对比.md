# 附加分析：开发侧基准对比（Total 611→525 / Comb 358→277）

- Commit：`244baaf19c8abf62443aabfb5ff449ff535a447d`
- Run ID：`244baaf-independent-v1`
- 性质：本文件由独立验证执行方在自动报告之外**追加**的分析，不修改任何既有产物；
  文件哈希已追加至 `ARTIFACTS.sha256`（追加行，原清单行保持不变）。

## 1. 结论摘要

| 口径 | Total cells | Comb cells | 与开发侧声明 |
|---|---|---|---|
| 开发侧声明 | 611 → **525** | 358 → **277** | 基准 |
| 独立测量：冻结官方流程（`proc; pmux_opt; opt_clean`） | 611 → **526** | 358 → **278** | 差 1 个 cell |
| 独立测量：推荐流程（`proc; opt; pmux_opt; opt_clean`） | 611 → **525** | 358 → **277** | **完全一致** |

**差异来源已定位，非随机误差：**

1. 本提交新增的 Pattern D（守卫条件驱动的比较器位宽缩减）**要求 `pmux_opt` 之前先执行 `opt`**。
2. 项目冻结的官方评测框架（`scripts/official_eval/run_case.tcl`，即 `tools/official_benchmark.py` 所用流程）
   在 `proc` 之后**直接**执行 `pmux_opt`，没有前置 `opt` → Pattern D 四例触发次数为 **0**，
   官方口径保持与 `6c5fcd0` 完全一致的 **526 / 278**（逐例 431/25/24/46）。
3. 在推荐流程下，仅 **test3** 多触发 3 次 `GUARD-AWARE EQ REWRITE`，映射后 test3 由
   `24 cells / comb 11` 降至 **`23 cells / comb 10`** → 聚合正好 **525 / 277**。
4. 开发侧 `scripts/run_scale_stress.sh` 与 `scripts/run_pattern_d_regression.sh` 均使用
   含前置 `opt` 的推荐流程，与 525/277 的来源一致。

## 2. 逐例映射级证据（本目录 8 个 log 由同版本工具链复算）

| Case | 官方流程 cells/comb | 推荐流程 cells/comb | 推荐流程 GUARD-AWARE 次数 |
|---|---|---|---|
| test1 | 431 / 207 | 431 / 207 | 0 |
| test2 | 25 / 25 | 25 / 25 | 0 |
| test3 | **24 / 11** | **23 / 10** | **3** |
| test4 | 46 / 35 | 46 / 35 | 0 |
| 合计 | **526 / 278** | **525 / 277** | 3 |

- 证据文件：`test{1..4}_official_mapped.log`、`test{1..4}_recommended_mapped.log`
- 流程内触发证据（第一阶段诊断，通用层）：
  `../../official/pair_swap_diagnostic/test3_official_flow.log`（GUARD-AWARE=0）、
  `../../official/pair_swap_diagnostic/test3_recommended_flow.log`（GUARD-AWARE=3）

## 3. 对 1000 节点与盲测的说明

- 本独立验证的 1000 节点三组与 8 例盲测均使用**冻结官方流程**，与官方评测口径一致：
  - mixed_1000 2341→1675（-28.45%）、pattern_a_1000 4150→2656（-36.00%）、negative_1000 2600→2600（无回退）
  - 该三组结果与 `6c5fcd0` 独立验证结果一致（官方流程下 Pattern D 不参与）。
- 若未来评测流程在 `pmux_opt` 前加入 `opt`，本提交的 Pattern D 将额外贡献（如 test3 的 1 个 cell）；
  该决策属于算法/评测负责人。

## 4. 判定影响

- 正确性：两种流程下 4/4 EQY PASS、Negative EQY EXPECTED_FAIL（rc=2），无任何影响。
- 资源：官方口径 **526/278（≥10% 达成、20% 未达成）**；推荐口径 **525/277（≥10% 达成、20% 未达成）**。
- 本次独立验证整体结论维持 **PASS**；本文件仅澄清“开发侧 525/277”的可复现条件。

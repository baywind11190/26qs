# 变更摘要（official4-p2 入口固化）

- 日期：2026-09-24；本记录目录：`results/90_整理记录_management/2026-09-24_230431_四例功能收尾与入口固化/`。
- 备份：`backup/`（46 个文件，含 runner/套件全量副本、WSL 已安装技能、共享自检缓存快照、未提交改动补丁）。
- 版本：流程 `official4-p1 → official4-p2`；模型 **`dffeas-v3`**；策略 **`noDF-local-r1`**；
  自检 **`official4-selfcheck-r2`**；套件 `official4-v1`（四例输入未变；清单增补 3 个自检素材条目）。
- 包清单：`PACKAGE_SHA256.json` **142 → 148 条**（17 项变化/新增，含 6 个新文件）。

## 一、入口改动

### 模型（`runner/form_cells_cycloneiv.v`）
- 替换为器件语义 v3（与修正轮 `06/02_model_v3/form_cells_cycloneiv_v3.v` **逐字节相同**，`d1f0e185…`）：
  clrn 异步清零 / ena 时钟使能 / power_up 初值。
- 支持配置预检强化（`runner/formal_status.py`）：power_up∈{low,high}、prn=1、asdata/aload/sclr/sload=0、
  **clrn/ena 等端口必须存在（缺失即阻断）**、未知单元/参数/端口取值阻断、纯组合（无 dffeas）合法；
  新增 `runner/precheck_supported_config.py`（空目录/缺预期文件即失败；未支持即阻断）。

### 策略（`noDF-local-r1`；`runner/function_common.py` + `run_eqy_chains.py` 重写）
- 流程：`eqy -m`（只生成分区与脚本）→ **严格转换**（仅删除恰一处 ` -set-def-formal`；逐字节核验；
  格式不符即停止）→ 本地执行 + 前提探针（noDF 必需 SAT；DF 记录历史签名）。
- 每轮从**该轮真实输入**重新生成并证明，不复用旧成功日志；判定联合（成功标记 + 基例数≥归纳长度 +
  断言覆盖 + 错误扫描；链级另加探针 SAT），不依赖单一 SUCCESS 字样。
- test3：**无切割整模块合并证明**（模板 `runner/miter/test3_miter.sv`，与 07 实验 miter `21aa9a59…` 逐字节相同；
  覆盖与轮内实际分区集合核对；maxsteps 16 / 600s 上限）；其余用例保留分区路径；两侧分别独立运行。

### 自检（`official4-selfcheck-r2`；`runner/run_selfcheck.py` 重写）
- 9 项：既有正/负例 + 初值一致/冲突 + 清零 + 使能 + LUT 正确性/改错 + **未声明初值“矛盾前提回归”**；
  全部经正式 noDF 管道执行。
- 指纹加入模型/策略版本与包文件哈希 ⇒ 旧缓存自动失效；同版本随后可复用（本轮已按新指纹更新）。

### 判定与报告（`report.py`）
- 联合判定 `row_functional_ok`；**每例两侧分别报告**（不使用“两链同结果”推断）。
- 新增功能复核报告：状态 `FUNCTION_RECHECK_COMPLETE`；标注“插件版功能补充复核”；内置验收始终 NOT_RUN。

### 编排（`validate.py`）
- 新参数 `--function-only --frozen-round <轮次>`：只 build + formal_selfcheck + chains；
  冻结输入**逐文件对照该轮 ALL_SHA256.txt**，失败即停（不回退完整 run）；四个非功能阶段记 NOT_RUN（原因入 stages）。
- 预检新增**脚本包清单核对**（`verify_package_manifest`）：内容不符/缺失/未登记即停止，不静默刷新。

### 文档与索引
- `POLICY.md` / `SKILL.md` 升级为 p2（三种形态与调用、双侧报告口径）；已同步 WSL 已安装技能。
- `08_四用例证据索引.md`：43×2/1×2/5×2/4×2 已标注为**两侧合计**并分别指向证据；补 c4b 合并证明；
  去除“两链同结果”笼统表述。

## 二、第一步：test3 原版对照（c4b）合并证明

- 目录：修正轮 `07_去掩蔽对照与合并证明/09_test3_c4b_原版对照_合并证明/`。
- 结果：由第03轮冻结 baseline 网表 + v3 模型经 `eqy -m` 重建合并模块（`b7b72f73…`；归一化后与 06 重跑 c4b `diff=0`）；
  前提探针 **PREMISES_SAT**；合并证明 **PASS@k14**（独立运行、重跑一致、四断言覆盖、无浅反例）。

## 三、第三步：功能复验运行（本记录验证轮）

- 轮次：`results/官方口径评测/2026-09-24_四例功能复核_5c44e87_第01轮/`。
- 结果：**FUNCTION_RECHECK_COMPLETE**（build / formal_selfcheck / chains rc=0；
  flowcheck / measurement_selfcheck / public / performance = NOT_RUN）。
- 功能（冻结第03轮输入；32 文件哈希全匹配；支持配置预检通过）：
  - test1：c4a 43/43、c4b 43/43（分区，探针全 SAT）；test2：1/1 ×2；test4：5/5 ×2；
  - test3：c4a 合并 PASS@k11、c4b 合并 PASS@k14（**两侧独立运行**；探针 SAT、四断言覆盖；
    分区级 4×2 已知伪影 FAIL 保留为辅助记录，不作链级结论）；
  - c1/c2 定位链亦全 PASS（test1 47/47 等）。
- 自检：9/9 符合预期（含未声明初值：noDF 前提 SAT、DF 前提 UNSAT 历史签名复现）。
- 轮次全量哈希：`11_汇总与证据_summary/ALL_SHA256.txt`（4181 行）；报告：`00_本轮验证结论.md`。
- 共享自检缓存已按新指纹更新（2026-09-24 23:18:38）。

## 四、与历史记录的关系

- 第03轮、修正轮、缺口复核等旧轮次与旧审计快照**全部保留、未改写**；
  本记录与`09`目录、复核轮的哈希清单为**补充覆盖范围**，不覆盖旧快照。
- 恢复方法：见 `恢复说明.md`；修改前基线：见 `清单.md`；未提交改动补丁：`backup/git_uncommitted/uncommitted.diff`。
- 边界：未重新综合、未测性能、内置验收 NOT_RUN、未继续 BMC256、未扩大到 H1/H2/规模、未提交/推送。

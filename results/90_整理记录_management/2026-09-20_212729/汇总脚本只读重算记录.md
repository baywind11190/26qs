# 汇总脚本只读重算记录（2026-09-20）

按迁移说明要求：不运行完整验证、不覆盖旧统计结果；将修编后的汇总脚本复制到
`/tmp/pmux_migration_check/`，仅把输出重定向到临时目录后执行，并与轮次内既有
统计文件逐字节比较。

| 脚本 | 读取路径（整理后） | 重算输出 | 与既有文件比较 |
|---|---|---|---|
| `make_summary.py` | `05_公开测试结果_public/`（24 组） | `out_summary.csv` / `out_summary.md` | 与 `summary.csv` 逐字节一致（SAME） |
| `make_formal_summary.py` | `06_公开功能验证_formal_public/`（16 组） | `out_formal.csv` / `out_formal.md` | 与 `formal_summary.csv` 逐字节一致（SAME） |
| `summarize_performance.py` | `09_时间内存与资源_performance/`（42 组） | `out_perf.csv` | 与 `performance/summary.csv` 逐字节一致（SAME） |

- 结论：脚本读取路径与报告输出名称已正确适配；未修改任何既有统计结果。
- 说明：`make_formal_summary.py` 的输出报告名改为 `公开功能验证汇总.md`（原
  `formal_summary.md`）；`make_summary.py` 输出报告名改为 `公开资源阶段汇总.md`。
  本次重算已验证新名称的写出逻辑正常。

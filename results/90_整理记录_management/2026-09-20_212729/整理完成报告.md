# 整理完成报告（WSL 验证结果目录整理与迁移）

- 执行日期：2026-09-20（批次 `2026-09-20_212729`）
- 依据：`01_WSL验证结果目录整理与迁移_DeepSeek执行说明.md`（用户已确认方案）
- 范围：`/home/fpga/fpga/pmux-opt/results` 与 `/home/fpga/fpga/pmux-validation`
- 一句话结论：**H1 轮次与全部历史结果已迁入 `results/` 中文分类结构，逐文件
  SHA-256 核对通过；本轮未重新验证 H1，也未将条件性通过升级为官方验收通过。**

## 1. 迁移内容与备份

| 项目 | 内容 |
|---|---|
| 迁移轮次 | 公开评测 13 轮（02 分类）、独立验证 2 轮（03 分类）+ 历史压缩归档 1 个、早期调试根文件 4 个（04 分类）、基础基线与等价日志（01 分类） |
| H1 轮次 | `/home/fpga/fpga/pmux-validation/h1-0c0ee40-NWBcQme2` 整轮迁入（126,868 文件） |
| results 历史 | 110,360 文件全部映射到新路径（0 缺失） |
| 迁移方式 | 同文件系统重命名/移动（先完成外部备份验收，再移动）；无复制副本残留 |
| 外部备份 | `/home/fpga/fpga/pmux-validation-backups/2026-09-20_212729/`：`results_迁移前快照/`（137,802 项）、`pmux-validation_迁移前快照/`（159,052 项）、`项目脚本与配置_迁移前快照/` |
| 源目录收尾 | 全部文件确认迁出并核对后，空壳目录 `/home/fpga/fpga/pmux-validation/` 已移除（仅剩空目录时） |
| 备份验收 | 快照与源逐文件比对：**0 缺失、0 内容不一致、0 mtime 差异**（见 `备份核对_基础与历史结果.md`、`备份核对_H1轮次.md`） |

## 2. 本轮 H1 的新目录与最终报告入口

- H1 新目录：`results/03_独立验证_independent_validation/2026-09-20_H1_0c0ee40_第01轮/`
- **最终报告入口**：`00_本轮验证结论.md`（11 项核对 + 总结；结论=条件性通过（团队口径））
- 目录说明：`01_本轮目录说明.md`（01~13 分类用途、查看路线、脚本清单）
- 顶层导航：`results/00_目录导航.md`（先看这里）

## 3. 新旧名称映射

- 完整映射（110 行，含归类理由/日期依据/提交依据/内容是否改变/处理状态）：
  - `新旧路径对照.csv`（导入表格用）
  - `新旧路径对照.md`（阅读用）

## 4. 修改过的脚本与报告（引用修编，内容未改结论）

全部 19+1 处修改逐条记录在 `引用修编记录.csv`（含改前/改后 SHA-256）与
`允许改动清单.md`。摘要：

| 类别 | 文件 | 修改原因 |
|---|---|---|
| H1 脚本 7 个 | `env.sh`、`run_flow.sh`、`make_summary.py`、`make_formal_summary.py`、`summarize_performance.py`、`selfcheck/run.sh`、`selfcheck-final/run.sh` | 目录路径（TASK_RUN/插件/读取目录/输出报告名/自检相对层级）适配 |
| H1 报告 8 个 | `00_本轮验证结论.md`、`公开资源阶段汇总.md`、`交付内容说明.md`、`公开功能验证报告.md`、`公开功能验证汇总.md`、`H1专项验证报告.md`、`开发侧参考结果.md`、`性能与资源报告.md` | 目录/文件引用更新 + 迁移说明注记 |
| 结果历史报告 4 个 | PatternD 轮 `00_本轮验证结论.md`、`附加分析_开发侧基准对比.md`；稳定版轮 `00_本轮验证结论.md`、`隐藏风格测试报告.md` | 同上（嵌套副本与目录树引用） |
| 项目文件 5 个 | `.gitignore`、`tools/official_benchmark.py`、`tools/collect_baseline.py`、`scripts/stat_pmux.ys`、`.vscode/tasks.json` | 输出/读取路径适配（评测输出 → `02_公开评测_public_eval`；调试任务输出 → `04_早期调试_early_debug`） |
| 文档 1 个 | `docs/benchmark_env.md` | 基线输出路径同步（1 行 + 迁移说明） |

## 5. 入口状态（哪些可用、哪些仅归档）

**已适配、可继续使用**：

- H1：`env.sh`、`run_flow.sh`、`run_formal.sh`、`run_formal_long.sh`、
  `make_summary.py`、`make_formal_summary.py`、`summarize_performance.py`、
  `11_验证工具自检_selfcheck/*/run.sh`（均从脚本自身位置定位 `env.sh`；自检脚本已
  适配加深一层的目录）。
- 项目：`tools/official_benchmark.py`（--run-id 输出至 `results/02_公开评测_public_eval/`）、
  `tools/collect_baseline.py`（读取 `01_基础基线_baseline/`）、
  `.vscode/tasks.json` 的构建/调试任务（调试输出写入 `04_早期调试_early_debug/`）。

**仅存档、勿直接续跑（含旧绝对路径，属历史证据）**：

- 各轮次内 `verify.eqy`、`flow.ys`（运行输出指向旧位置）、EQY `verify/` 与
  `strategies/` 工作目录、历史日志与 `ARTIFACTS.sha256`/`ALL_SHA256.txt`/`MANIFEST.txt`
  （清单内路径为迁移前路径，换算用映射表）。
- 各独立验证轮内嵌套的公开评测副本已按同规则改名（`summary.md → 公开资源汇总.md` 等），
  未建立旧名软链接（避免重复入口）。

**个人 Skill（未修改，按要求仅在报告中列出）**：

- `pmux-commit-validation/SKILL.md`：仍引用旧的 `results/official_eval/<run-id>/...`
  产物路径；
- `pmux-independent-validation/SKILL.md`：仍按 `results/independent_validation/<short>-independent-vN/`
  创建/读取独立验证目录。
- 两处均**未改**（未经本任务必要性证明）；后续如继续使用这两个 Skill，其产生的目录需按
  `00_目录导航.md` 的“以后的新实验规则”归并，或另行授权后再适配。

## 6. 核对与检查结果（全部实测）

| 检查 | 结果 |
|---|---|
| 备份验收（源 vs 快照，逐文件） | **PASS**：results 137,802 项、pmux-validation 159,052 项，0 差异 |
| 迁移核对 H1（含 EQY 工作目录，逐文件 SHA-256） | **PASS**：126,868 文件，0 缺失、0 不一致；15 个“允许改动”文件在位（`迁移核对_H1轮次.md`） |
| 迁移核对 历史 results | **PASS**：110,360 文件，0 缺失、0 不一致；4 个“允许改动”文件在位（`迁移核对_历史结果.md`） |
| Bash 语法（`bash -n`） | 6 个脚本全部通过 |
| Python 语法（AST） | 5 个项目/轮次脚本 + 批次脚本全部通过 |
| 路径解析 | `env.sh` 源用有效（TASK_RUN/插件 h1.so+parent.so/公共目录）；两个自检脚本三层回溯找到 `env.sh` |
| 汇总脚本只读重算（临时目录，不覆盖旧结果） | 3 个脚本重算输出与既有 CSV 逐字节一致（`汇总脚本只读重算记录.md`） |
| 新导航/说明的相对链接 | 19 个文件，0 失效链接 |
| `.gitignore` 代表文件（`git check-ignore`） | 报告 .md/摘要 csv/新旧路径对照.csv 可入库；`.so`、stat JSON、`tar.gz`、重型清单保持忽略 |
| git 状态（前后对比） | 整理前干净；整理后仅本次预期变更（89 D 重命名删除 + 5 M 脚本 + 未跟踪新文件）；**未做任何 git add/commit** |
| 算法/RTL/工具 | `git diff` 对 `src/`、`pmux_case/`、`tests/`、`formal/` 为空；工具版本未动 |

## 7. 仍待确认的信息（如实保留，未推断升级）

- `04_早期调试_early_debug/` 两个分组的**日期与提交待确认**（仅 mtime 线索：2026-09-12 /
  2026-09-16；早于仓库首个提交，可能受复制影响）。
- 公开评测中 `等价自检test2`、`等价自检全例` 两轮的**提交待确认**（统一评测框架提交
  `e984a59` 之前的调试卷，无 manifest）。
- `b600a03` 两轮 smoke 的运行发生在统一评测框架提交前（工作树含未提交框架改动），
  manifest 记 `project_commit=b600a03`，按记录保留。
- 独立验证内 `STABLE` 基准归属（`08_H1专项结果_h1_results/开发侧参考结果.md` 原样保留该
  未确认警告）；开发侧 RTL 未交付、“开发用例独立复现”为 **NOT_RUN**（属验证内容，不属
  本次整理范围，未改变其状态）。

## 8. 未完成项

- 本次**未重新执行**任何验证（未跑 EQY/Yosys 全流程、未重编译插件）；未将任何
  FAIL/TIMEOUT/NOT_RUN/条件性通过改写为 PASS。
- 未改动两个个人 Skill（见第 5 节）。
- 未提供各历史轮次的“复跑配置副本”：历史 `verify.eqy`/`flow.ys` 保留原字节作证据；
  复跑统一走已适配的 H1 脚本入口（避免重复入口与误覆盖历史证明目录）。
- 重型清单 `迁移后文件清单.json` / `迁移后SHA256.txt`（约 63.5 MB / 57.5 MB）保留在本
  批次目录中，按 `.gitignore` 保持忽略（与 `ARTIFACTS.sha256` 同策略），如需入库请显式
  `git add -f`。

## 9. 回退方法

前提：**先停止相关写入**；**先给现有新目录做一份整体备份**；不要用一条命令直接删除新
目录；目标有冲突时不覆盖。

1. 结果树回退（回到 2026-09-20 整理前）：

   ```bash
   # 1) 备份当前新结构（示例，目标名勿冲突）
   rsync -a /home/fpga/fpga/pmux-opt/results/ /home/fpga/fpga/pmux-validation-backups/回退前_results/
   # 2) 用快照覆盖回来前先对照：详见 新旧路径对照.csv（旧←新）
   rsync -a --delete /home/fpga/fpga/pmux-validation-backups/2026-09-20_212729/results_迁移前快照/ \
       /home/fpga/fpga/pmux-opt/results/
   ```

2. H1 轮次回退：

   ```bash
   rsync -a /home/fpga/fpga/pmux-validation-backups/2026-09-20_212729/pmux-validation_迁移前快照/ \
       /home/fpga/fpga/pmux-validation/
   ```
   （即恢复 `/home/fpga/fpga/pmux-validation/h1-0c0ee40-NWBcQme2/`。）

3. 项目脚本/配置/文档回退：从 `项目脚本与配置_迁移前快照/` 覆盖回 `scripts/`、`tools/`、
   `.vscode/`、`.gitignore`；`docs/benchmark_env.md` 用 `git checkout -- docs/benchmark_env.md`
   恢复（其改前哈希 `3852908323bf92ee...`，见 `引用修编记录.csv`）。

4. 校验：回退后可用 `备份核对_*.md` 与 `迁移核对_*.md` 的口径重新生成清单比对；
   引用修编的全部改前/改后 SHA-256 在 `引用修编记录.csv`，可逐文件比对。

## 附：本批次文件清单（`results/90_整理记录_management/2026-09-20_212729/`）

| 文件 | 说明 |
|---|---|
| `整理计划.md` | 整理计划与边界 |
| `新旧路径对照.csv` / `.md` | 新旧路径完整映射（110 行） |
| `备份核对_基础与历史结果.md` / `备份核对_H1轮次.md` | 备份逐文件核对报告 |
| `迁移核对_H1轮次.md` / `迁移核对_历史结果.md` | 迁移后逐文件核对报告 |
| `引用修编记录.csv` | 20 个文件的内容修编记录（含改前/改后哈希） |
| `允许改动清单.md` | A 内容修编 / B 新增文件 / C 重命名 / D 项目文件 / E 未改动 |
| `汇总脚本只读重算记录.md` | 3 个汇总脚本临时目录重算结果 |
| `迁移后文件清单.json` / `迁移后SHA256.txt` | 整理后定稿清单（含排除项声明；量大，git 忽略） |
| `整理完成报告.md` | 本报告 |
| `results_迁移前清单.tsv` 等 4 个 .tsv | 备份/核对用源与快照清单 |
| `build_manifest.py` 等批次脚本 | 可复现核对流程 |
| `迁移操作日志.txt` | 每一步移动/改名的带时间戳日志 |

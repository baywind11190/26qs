# 05 结论汇总（器件语义模型 v3 · 独立实验）

- 固定算法提交：`5c44e87ec403a4e2d7f961e09a3cae6f09919074`；已有网表、官方输入、算法、主入口未改动；
  不新增复位假设、不重跑性能、不提交推送；旧证据全部保留。
- 本汇总按三部分：**模型检查结果 / 四例证明结果 / 仍未解决的问题**。

## 一、模型检查结果（真实检查，金侧声明初值）

| 项 | 结果 |
| --- | --- |
| 清零（clrn 低=异步清零） | v2 缺路径 → 反例检出；v3 → PASS（最小对照 chk_clr） |
| 使能（ena=0 保持） | v2 缺路径 → 反例检出；v3 → PASS（chk_ena；本轮网表 200+ 实例 ena 为信号驱动） |
| 初值 | 声明一致 → PASS；声明冲突 → 检出（d0_low/d1_high/d0_high 三向对照） |
| 控制优先级 | 清零优先于使能/时钟装载（chk_clr 含 ena 场景） |
| 未声明初值（金侧） | **当前协议下基例被掩蔽**：`-set-init-undef`（Sx）与 `-set-def-formal`（`satgen.cc:1206-1221` 初值已定义假设）冲突 → 含“未声明初值普通 FF”的设计基例整体 UNSAT；`$anyinit` 不触发（`sat.cc:265-272` forced_def） |
| 掩蔽决定性证据 | T1（去 def_formal → 可证伪）vs T2（全 flags → 掩蔽）；T1b（v2 检出使能缺失）vs T1c（v3 通过）；无容忍项版本 C2 同被掩蔽（掩蔽与容忍项独立） |
| 支持配置预检 | 493 个 dffeas 全部通过；`prn/sclr/sload/aload/asdata/ power_up` 违例即阻断（未触发） |

## 二、四例证明结果（v3 重跑，8 链；c4a≡c4b）

| 用例 | 结果 | 真实性 |
| --- | --- | --- |
| test1 | **43/43 PASS** | 3 真实（state / por6_out / inter0007_out，由真实 FAIL→真实 PASS）；40 掩蔽 PASS |
| test2 | 1/1 PASS | 真实 |
| test3 | sr_out PASS、sr/gpioout UNKNOWN、rw FAIL | 前三者掩蔽；**rw 为真实检查的“分区构造伪影”**（两半切割 `sr[7]≡rw` 别名；非设计/网表/算法/模型缺陷） |
| test4 | 5/5 PASS | 全部掩蔽 PASS（含 dataout：UNKNOWN→PASS） |

- 与历史：test1 的 40 UNKNOWN + 3 FAIL 全部转为 PASS；test4.dataout 转 PASS；test3 无变化。
- 解读：v3（清零+使能按器件语义）修复了此前唯一的 3 个真实反例；其余“PASS”中 96/106 个分区的基例受掩蔽（归纳成立）。

## 三、仍未解决的问题（按优先级）

1. **test3.rw 伪影的处理**：分区生成对别名网（`sr[7]≡rw`）的门侧切割不完整；可选：标注为已知伪影 / 工具层修复分区生成 / 以 sr_out 等价链覆盖。
2. **掩蔽（96/106 分区）**：基例未被检查。是否执行“去掩蔽”（仅去 `-set-def-formal`）的受控对照重跑（最小对照已证明可行）——待批准；该操作会改变验证协议，故本轮未做。
3. **test3.sr/gpioout**：深度 5 未闭合（掩蔽下 UNKNOWN；gpioout 历史诊断需归纳长度 6）——深度 policy 未变。
4. **v3 是否采纳**：当前仍在独立实验目录（`06_器件语义模型_v3/`），未入主入口；初值方案 A 未采纳、未实施。

## 附：本目录产物索引

- `01_最小对照_初值处理/`：Q1/Q2/Q3 对照 + `00_结论.md`（含掩蔽发现）
- `02_model_v3/`：`form_cells_cycloneiv_v3.v`、`00_v3说明.md`、`precheck_supported_config.py`
- `03_模型检查/`：`model_checks.v`、`run_model_checks.sh`、`00_结论.md`、`掩蔽期记录/`（旧假象日志）、掩蔽证据日志（T1/T2/T1b/T1c/C*/E*/dbg_*）
- `04_四例重跑/`：`run_reproof_v3.py`、`run01/`、`precheck_report.txt/json`、`mask_audit.csv`、`reproof_v3_summary.json/csv`、`00_四例重跑结论.md`
- 轮根 `ALL_SHA256.txt`：含本目录全部文件哈希

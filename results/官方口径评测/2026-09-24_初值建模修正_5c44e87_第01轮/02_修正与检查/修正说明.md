# 修正说明（模型 v2）与针对性检查

## 1. 修正内容（最小范围）

- 文件：`form_cells_cycloneiv_v2.v`（= v1 + 一处新增；v1 快照见 `../01_证据数据/form_cells_cycloneiv_v1.v`）。
- 新增：`dffeas` 的上电初值建模——
  `power_up == "low" → q_tmp 初值 0`；`"high" → 初值 1`；其他（含默认 "dontcare"）→ 不声明初值（自由）。
- 未改动：aload=0 假设、lcell 重建、其他近似；**未**修改任何证明脚本（EQY 流程原样）。
- **状态**：v2 未进入主入口（`scripts/pmux_validation/runner/form_cells_cycloneiv.v` 未改动）；
  是否采纳、以及配套的口径决策（见 00 文档一.4 待决项）另行决定。

## 2. 变体对照（证明两条释放语句当前为无操作）

`../03_重跑受影响证明/变体对照/`：对 test1.state 分别
（a）去掉 `-ff2anyinit`；（b）再去掉 `setundef -anyseq` 行；
结果与标准运行**逐行一致**（同为 base case 1 通过、base case 2 FAIL）。
→ `formalff -ff2anyinit` 仅作用于"未初始化"位（其文档语义），在 v2 提供初值后不再释放该 FF；
故**除模型外无需修改证明配置**。

## 3. 针对性检查（错误初值检出能力）

- 静态一致性审计：`针对性检查/初值一致性审计.csv`（test1 三条声明信号逐条核对一致；含 inter0007
  反相实现；test2/3/4 无声明 + 网表默认机制注记）。
- 动态检出对照（`针对性检查/mini_designs.v`、`run_init_check.sh`，配方与链内策略同型）：

| 对照 | 构造 | 结果 | 含义 |
| --- | --- | --- | --- |
| `top_match` | gold `initial=1` vs `power_up="high"` | PASS（归纳步成功） | 一致初值不误报 |
| `top_mismatch` | gold `initial=1` vs `power_up="low"` | **FAIL（基例 1 命中，检出）** | 错误初值可被检出 |

- 对照不使用任何"指定寄存器值获得通过"手段：两侧初值分别来自 RTL 声明与网表声明。

## 4. 复现方式

```bash
cd 02_修正与检查/针对性检查 && bash run_init_check.sh     # 期望：top_match=PASS；top_mismatch=FAIL_DETECTED
# 重跑（生成新 run 目录时）：
cd 03_重跑受影响证明 && python3 run_reproof.py            # 注意：run01 已存在时会拒绝覆盖
```

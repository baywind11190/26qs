# 官方四例插件预检

执行状态：**NEEDS_REVIEW**。
形态：四例插件预检（显式 `--mode plugin`）；**最终内置四例验收：NOT_RUN**——插件结果不构成内置验收。

证据根目录：`/home/fpga/fpga/pmux-opt/results/官方口径评测/2026-09-24_稳定版-四例_5c44e87_第02轮`；功能证明链采用 `run01`。

- 算法提交：`5c44e87ec403a4e2d7f961e09a3cae6f09919074`
- 项目 HEAD：`817edf7d7ce35ef8ccf7c280ddccc962840e6c94`
- 源码 SHA-256：`e3663d90f97397475188cf147c272b5e1194e923399760a73ad4a8cebe3d24be`
- 插件 SHA-256：`ceaa0ad21a185c4ab7fb24322c76ee5030db7a439d24647023af72ec94e32687`
- 流程 / 套件：official4-p1 / official4-v1
- 链集合：main（main=主路径+对照+定位；full=全部链）

## 检查结果

| 项目 | 结果 |
|---|---|
| 执行阶段完整 | 未满足或缺证据 |
| 插件构建与隔离 | 满足 |
| 四例综合与检查 | 未满足或缺证据 |
| DFF逐例不增加 | 未满足或缺证据 |
| 功能证明（默认链集合） | 未满足或缺证据 |
| 四例性能逐例判定 | 未满足或缺证据 |
| 四例输入指纹已核验 | 满足 |

## 四例资源与缩减率

| 用例 | Comb 前→后 | DFF 前→后 | Comb 缩减率 |
|---|---|---|---|

四例 Comb 缩减率算术平均：不可计算（缺测不缩分母）%。
逻辑单元口径为 `cycloneiv_lcell_comb`；DFF 独立检查、不应增加；Total 仅辅助。

## 功能证明状态

默认链集合说明：`c4a_rtl_opt_mapped`（原始 RTL ↔ 优化版最终映射网表）为主路径；`c4b_rtl_base_mapped` 为 baseline 对照；`c1_local`（P3 局部）与 `c2_stage`（同阶段）是范围受限证据与失败定位，不能单独支撑全链声明。
完整公开证明链：NOT_CLOSED。
- 默认链集合全部通过。
- 未运行链：c3a/c3b/c5（`--chains full` 可单独运行；默认不作为每轮必跑）。
- COUNTEREXAMPLE_REQUIRES_REVIEW 表示工具报告分区不等价，需复核配置、假设和反例，不自动断言算法错误；MODEL_UNSUPPORTED 表示映射模型适用条件不满足，保留条件、不强行证明。

## 四例性能（逐例判定）

| 用例 | CPU 中位数前→后(s) | CPU 开销 | CPU 5% | RSS 前→后(KiB) | 内存开销 | 内存 5% | 60s/2GB | 样本有效 |
|---|---|---|---|---|---|---|---|---|

每侧五次交错运行、取中位数；相对开销 =（优化版-原版）/原版，逐例与 5% 比较（边界值按不超过处理；不跨例平均、不使用舍入后的显示值）。
CPU 时间为 wait4 采集并覆盖子进程树（含 ABC 子进程）；测量自检含孙进程 CPU 累计检查。
内存为 5ms 间隔进程组 RSS 求和采样；可能漏掉短峰值、重复计入共享页、包含包装进程；不是精确官方内存计量。
60s/2GB 以原版为对照；原版超限时允许优化版也超限；不自行发明扣分计算。
无效样本（含工具失败、超时、缺失）不判定通过；原始样本全部保留、不自动剔除。

## 证据

- `validation_context.json`：工具、提交、四例输入套件、代码指纹与隔离检查结果。
- `01_来源与环境_meta/stages.json`：本阶段状态；自检阶段带 `REUSED` 标记时表示按指纹复用。
- 本报告清单：`/home/fpga/fpga/pmux-opt/results/官方口径评测/2026-09-24_稳定版-四例_5c44e87_第02轮/11_汇总与证据_summary/manifest.json`。
- 全量证据哈希：`/home/fpga/fpga/pmux-opt/results/官方口径评测/2026-09-24_稳定版-四例_5c44e87_第02轮/11_汇总与证据_summary/ALL_SHA256.txt`（生成于运行收尾）。

执行状态与缺证据/需复核一致；阶段完成不等于功能或性能通过；没有擅自补充官方分数。

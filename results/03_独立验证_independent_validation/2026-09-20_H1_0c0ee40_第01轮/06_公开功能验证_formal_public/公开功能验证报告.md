# 02｜Formal 分层与结果判读 — 执行报告（H1 / 0c0ee40）

> 迁移说明（2026-09-20 目录整理）：本文件内的目录/文件引用（如有）已按整理后的中文目录名更新；实验内容与结论未改动。

执行日期：2026-09-20　TASK_RUN（整理后）：`/home/fpga/fpga/pmux-opt/results/03_独立验证_independent_validation/2026-09-20_H1_0c0ee40_第01轮`
工具：EQY v0.69（`/home/fpga/eda/eqy/install/bin/eqy`，Yosys 固定为 baseline 0.69 9f75ca1f9）　策略：simple / sat / depth 5

## 1. H1 在 P 流程下的 baseline-generic vs H1-generic（pairwise，团队主方法）

| 用例 | 结果 | 分区 | 备注 |
|---|---|---|---|
| test1 | **PASS** (rc=0) | 47/47 | 含 state/i0-i7 分区 |
| test2 | **PASS** (rc=0) | 6/6 | |
| test3 | **PASS** (rc=0) | 6/6 | 含 sr、sr_out、rw、gpioout |
| test4 | **PASS** (rc=0) | 5/5 | 两侧 memory_map |

结论：共同前后处理（proc;opt;…;opt）下，加入 pmux_opt(H1) 的**净变化被证明等价**，4/4 PASS。
（对比：244baaf 的 test3 pairwise PASS 属旧版本，本结果是 H1 重新执行的。）

## 2. R1 RTL 检查（官方口径 proc; pmux_opt; opt_clean）

| 用例 | 结果 | 分区 | 备注 |
|---|---|---|---|
| test1 | **PASS** (rc=0) | 47/47 | 含 por6、inter0008、inter0009 |
| test2 | **PASS** (rc=0) | 6/6 | |
| test3 | **PASS** (rc=0) | 4/4 | 含 test3.sr |
| test4 | **PASS** (rc=0) | 5/5 | 两侧 memory_map |

结论：官方口径下 R1 H1 generic 与原始 RTL 等价性 4/4 PASS（62 分区全证），**H1 已重新确认**（历史 PASS 属旧版本，未继承）。

## 3. P RTL 检查（原始 RTL vs P 网表；含无插件 baseline 对照）

| 用例 | baseline（无插件） | H1 | 失败分区对比 |
|---|---|---|---|
| test1 | NEEDS_REVIEW rc=2；44/47 | NEEDS_REVIEW rc=2；44/47 | **完全相同**：por6(not equivalent)、inter0008/inter0009(unknown) |
| test2 | PASS 6/6 | PASS 6/6 | — |
| test3 | NEEDS_REVIEW rc=2；3/4 | NEEDS_REVIEW rc=2；3/4 | **完全相同**：sr(unknown) |
| test4 | PASS 5/5 | PASS 5/5 | — |

对照分析（证据见 `06_公开功能验证_formal_public/*/verify.log` 与各 `strategies/*/simple/run.log`）：

1. **失败不依赖插件**：baseline 与 H1 的失败分区集合、SAT 问题规模（如 por6 base case 2：1516 vars / 4232 clauses）、日志行号（180/181/292/333/334）完全同构。
2. **同一分区在 R1 口径全部可证**（第 2 节，含 por6、inter*、sr）→ P 流程失败与 "RTL vs 含 opt 的网表" 组合有关（opt 重编码），而非设计或插件差异。
3. **por6 模型复核**：反例（t2: state=1101/CYCLE13、reset=0、gold.por6=1 vs gate.por6=0）与 RTL 语义矛盾——test1.v 中 CYCLE1-15 全部分支 `por6 <= 1'b0`、reset 分支保持、初值 0，`por6` 不可能在非 START 状态为 1 → 该模型**不构成有效设计级反例**，属验证口径伪影（与 244baaf 时期对同类现象的归因一致：opt 的 $dff→$dffe 重编码 + EQY 分区建模局限；proc-only 曾可证，proc;opt 起不可证且无插件相同）。
4. 分类记录：P-test1/test3 的 RTL 项 = **UNPROVEN**（就"RTL-vs-P-opt网表"命题；含 1 个复核不成立的模型 + 2+1 个 unknown）；就"插件未引入差异"命题：baseline 同构 + R1 全证 + pairwise 全证，**无插件嫌疑**。

## 4. 限制与声明（不写为 PASS 的部分）

- generic 层结果 ≠ mapped 层；本报告不宣称 mapped-vs-RTL 已验证，若验收指定映射网表需另建含 Cyclone IV 库模型的配置。
- pairwise 为团队主 Formal 方法，**官方接受方式待确认**；RTL 检查（R1 口径）4/4 PASS 对团队方法是重要补充。
- 未执行项（不在本报告 PASS 之列）：hidden 边界覆盖、1000-node Formal、性能与资源稳定性。
- 所有失败项均已保留原状证据，未添加任何输入假设、未删除失败输出、未放宽 X 处理、未跳过分区。

## 5. 证据索引

- 数据表：`06_公开功能验证_formal_public/公开功能验证汇总.md`、`06_公开功能验证_formal_public/formal_summary.csv`
- 每组：`06_公开功能验证_formal_public/<组>/`：`verify.eqy`（配置）、`inputs.sha256`、`actual_yosys.txt`（固定工具）、`exit_code.txt`、`classification.txt`、`verify.log`、`verify/`（EQY 工作目录，含分区与反例 trace）
- por6 反例：`06_公开功能验证_formal_public/P-test1-RTL-h1/verify/strategies/test1.por6/simple/{run.log,trace.vcd}`

# 2026 FPGA 赛道｜PMUX 优化项目——独立验证工作交接

## 一、项目背景

项目：

```text
2026 全国大学生嵌入式芯片与系统设计竞赛
FPGA 创新设计赛道
中科亿海微——选题二：PMUX 结构优化
```

仓库：

```text
https://github.com/baywind11190/26qs
```

本地工程：

```text
/home/fpga/fpga/pmux-opt
```

主要环境：

```text
Ubuntu 24.04.5 LTS
Yosys 0.69
EQY v0.69
Z3 4.8.12
mapping family = Cyclone IV
```

主要路径：

```text
项目：
/home/fpga/fpga/pmux-opt

baseline Yosys：
/home/fpga/eda/yosys-baseline/build/yosys

optimized Yosys：
/home/fpga/eda/yosys-optimized/build/yosys

EQY：
/home/fpga/eda/eqy/install/bin/eqy
```

用户目前的角色不是算法开发负责人，而是：

```text
独立验证负责人
隐藏测试模拟负责人
```

核心原则：

```text
只验证 exact commit
不主动修改 src/pmux_opt.cc
发现异常先保留证据
不要为了让结果好看而改流程
不要把 EQY FAIL 直接等价成“算法错误”
```

---

# 二、比赛指标与当前统一口径

比赛关注：

```text
1. Formal 等价
2. Total cells
3. 节点 <=1000 时 runtime <=60s
4. peak RSS <=2GB
5. 支持混合组合逻辑 PMUX
```

资源主指标：

```text
Total cells
```

辅助指标：

```text
cycloneiv_lcell_comb
DFFEAS
```

面积参考：

```text
Total cells 缩减 >=10%：达到题目目标
Total cells 缩减 >=20%：面积满分目标
```

必须强调：

```text
Comb 不是官方总面积指标。
```

---

# 三、早期公开基线

公开 test1~test4 baseline：

```text
test1:
Total 510
Comb 286
DFF 222

test2:
Total 25
Comb 25
DFF 0

test3:
Total 30
Comb 12
DFF 17

test4:
Total 46
Comb 35
DFF 10
```

Aggregate：

```text
Total 611
Comb 358
DFF 249
```

---

# 四、之前已验证的版本

## 1. `9a5f7e4`

旧 Pair-swap 版本。

结果：

```text
test1:
510 → 431
286 → 207
222 → 222

test2:
25 → 25
25 → 25

test3:
30 → 30
12 → 12
17 → 17

test4:
46 → 46
35 → 35
10 → 10
```

Aggregate：

```text
Total 611 → 532
约 -12.93%

Comb 358 → 279
约 -22.07%
```

---

## 2. `4988eaa`

Pattern B 加入后：

```text
test3:
Total 30 → 24
Comb 12 → 11
DFF 17 → 12
```

Aggregate：

```text
Total 611 → 526
约 -13.91%

Comb 358 → 278
约 -22.35%

DFF 249 → 244
```

Negative EQY：

```text
EXPECTED_FAIL
rc=2
```

---

## 3. `6c5fcd0`

后续稳定版本。

公开结果仍为：

```text
Total 611 → 526
Comb 358 → 278
DFF 249 → 244
```

test1~4 全部 EQY PASS。

1000-node：

```text
mixed_1000:
约 995 nodes
Total 2341 → 1675

pattern_a_1000:
约 996 nodes
Total 4150 → 2656

negative_1000:
1000 nodes
Total 2600 → 2600
```

全部：

```text
EQY PASS
runtime 约 1s
RSS 约几十 MB
```

远低于：

```text
60s / 2GB
```

hidden-style 8 组也全部 PASS。

---

# 五、已经制作的两个 Agent Skill

已经做成第二版双 Skill：

```text
pmux-commit-validation
pmux-independent-validation
```

压缩包曾输出：

```text
pmux-validation-skills-v2.zip
```

## Skill 1：`pmux-commit-validation`

用途：

```text
给一个稳定 commit
→ exact source
→ build
→ test1~test4
→ EQY
→ Negative EQY
→ Total / Comb / DFF
→ time / RSS
→ 中文报告
→ 归档 / 恢复
```

---

## Skill 2：`pmux-independent-validation`

用途：

```text
调用第一层
→ public test1~4
→ 1000-node
→ blind01~blind08
→ crash / timeout / 非等价 / resource regression
→ 最终独立验证报告
```

但注意：

后续发现原 Skill 中的“主评测流程”需要更新，不能再单纯把：

```text
proc → pmux_opt → opt_clean
```

当成主资源评测流程。

最新统一口径见后文。

---

# 六、Pattern D：`244baaf`

Commit：

```text
244baaf19c8abf62443aabfb5ff449ff535a447d
```

内容：

```text
新增 Pattern D：
守卫条件驱动的比较器位宽缩减
```

算法负责人开发侧曾报告：

```text
Total 611 → 525
Comb 358 → 277
```

而最初独立完整验证得到：

```text
Total 611 → 526
Comb 358 → 278
```

恰好差：

```text
1 Total cell
1 Comb cell
```

后来确认额外收益来自公开 `test3`。

Pattern D 前：

```text
test3:
Total 30 → 24
Comb 12 → 11
DFF 17 → 12
```

Pattern D 后开发侧：

```text
test3:
Total 30 → 23
Comb 12 → 10
DFF 17 → 12
```

---

# 七、Pattern D 差异根因已经查明

两边使用了不同的 Yosys pass flow。

原独立冻结流程：

```text
read_verilog
→ hierarchy
→ proc
→ pmux_opt
→ opt_clean
→ synth_intel
```

在这个流程下：

```text
test3 = 30 → 24
Comb = 12 → 11
```

Pattern D 不触发。

算法负责人开发侧流程：

```text
read_verilog
→ hierarchy
→ proc
→ opt
→ pmux_opt
→ opt
→ synth_intel -family cycloneiv
→ stat
```

在这个流程下：

```text
test3 = 30 → 23
Comb = 12 → 10
DFF = 17 → 12
```

Pattern D 触发。

因此：

```text
Pattern D 对前置 opt 暴露出的网表结构有依赖。
```

旧流程没触发 Pattern D，不代表算法错误。

---

# 八、Pattern D 定向复测已经完成

使用：

```text
244baaf
fresh source
fresh pmux_opt.so
baseline Yosys
```

证据链：

```text
exact commit
→ git show src/pmux_opt.cc
→ source sha256
→ baseline 自带 yosys-config
→ fresh build pmux_opt.so
→ plugin sha256
→ baseline Yosys 本身无 pmux_opt
→ -m fresh pmux_opt.so
→ help pmux_opt 生效
```

定向流程：

```text
Baseline:
proc
→ opt
→ opt

Optimized:
proc
→ opt
→ pmux_opt
→ opt
```

映射：

```text
synth_intel -family cycloneiv
```

结果两次一致：

```text
Baseline:
Total 30
Comb 12
DFF 17

244baaf:
Total 23
Comb 10
DFF 12
```

也就是：

```text
Pattern D 相比旧版：
Total 再 -1
Comb 再 -1
DFF 不变
```

日志中明确出现：

```text
GUARD-AWARE EQ REWRITE ×3
old width 8 → new width 7
Pattern D candidates in module test3: 3
```

所以 Pattern D 收益已独立复现。

---

# 九、Pattern D EQY 的重要结论

曾经用：

```text
Gold = 原始 RTL

Gate =
proc
→ opt
→ pmux_opt
→ opt
```

做 EQY。

结果：

```text
test3.sr = equivalence unknown
整体 FAIL rc=2
```

但是进一步对照发现：

完全没有 plugin 时：

```text
RTL
vs
proc → opt → opt
```

仍然在：

```text
test3.sr
```

出现相同 FAIL。

进一步：

```text
proc                PASS
proc → opt          FAIL
proc → opt → opt    FAIL
```

说明问题来源于：

```text
Yosys opt / opt_dff
```

它会把类似：

```text
$dff
```

重编码为：

```text
$dffe
```

导致 EQY 对内部寄存器分区的 RTL-vs-opt 后网表验证出现 unknown。

因此：

```text
RTL vs opt 后 generic 的 EQY FAIL
不能直接归因给 pmux_opt。
```

---

# 十、现在统一的主评测流程 P

最新交接文件明确规定：

不要把任何流程称为“官方流程”。

因为组委会没有明确规定具体 pass 顺序。

统一叫：

```text
主评测流程 P
鲁棒性流程 R1
```

## P Baseline

```text
read_verilog
→ hierarchy
→ proc
→ opt
→ 不执行 pmux_opt
→ opt
→ synth_intel -family cycloneiv
→ stat
```

## P Optimized

```text
read_verilog
→ hierarchy
→ proc
→ opt
→ pmux_opt
→ opt
→ synth_intel -family cycloneiv
→ stat
```

原则：

```text
同一 RTL
同一 Yosys
同一前处理
同一后处理
同一 mapping
唯一变量 = pmux_opt
```

这是后续 QoR/资源评测的主流程。

---

# 十一、主流程 P 的 Formal 方法

不要再使用：

```text
原始 RTL
vs
proc → opt → pmux_opt → opt
```

作为主流程 Formal。

P Formal 应采用 pairwise：

## Gold

```text
proc
→ opt
→ opt
```

## Gate

```text
proc
→ opt
→ pmux_opt
→ opt
```

也就是：

```text
baseline_generic.il
vs
optimized_generic.il
```

Pattern D test3 已用此方法证明：

```text
sr PASS
sr_out PASS
rw PASS
gpioout PASS
```

因此：

```text
主流程 P：
pairwise generic-vs-generic Formal
```

---

# 十二、R1 鲁棒性流程

R1 保留旧插入方式：

```text
read_verilog
→ hierarchy
→ proc
→ pmux_opt
→ opt_clean
```

作用：

```text
检查无前置 opt 时：
是否 crash
是否 assert
是否误触发
Formal 是否安全
资源是否明显退化
```

这里可以用：

```text
RTL
vs
proc → pmux_opt → opt_clean
```

做 Formal。

Pattern D 已在 R1 PASS。

注意：

```text
Pattern D 在 R1 不触发
不等于 Pattern D 错。
```

---

# 十三、当前 P 参考值

Pattern D 后 P 流程 public test：

```text
test1:
Total 510 → 431
Comb 286 → 207
DFF 222 → 222

test2:
Total 25 → 25
Comb 25 → 25
DFF 0 → 0

test3:
Total 30 → 23
Comb 12 → 10
DFF 17 → 12

test4:
Total 46 → 46
Comb 35 → 35
DFF 10 → 10
```

Aggregate：

```text
Total 611 → 525
约 -14.08%

Comb 358 → 277
约 -22.63%

DFF 249 → 244
约 -2.01%
```

以后报告必须写清：

```text
面积主指标 = Total
Comb = 辅助
```

---

# 十四、旧结果不要被覆盖

以后必须带 flow 标签。

例如：

```text
R1 / 旧冻结流程：
test3 30→24 / 12→11

P：
test3 30→23 / 12→10
```

两者：

```text
同时保留
不能静默覆盖
```

---

# 十五、当前最新任务：H1

当前待独立验证 Commit：

```text
0c0ee40
```

提交说明：

```text
优化：扩展 PMUX 双特殊分支配对重构
```

父提交：

```text
244baaf
Pattern D
```

交接文件中特别提醒：

```text
远程是否已经包含 0c0ee40 最后没有明确确认。
验证前必须真实检查。
```

---

# 十六、H1 是什么

H1 是 Pattern A 的扩展。

旧 Pattern A：

```text
最多容忍 1 个 special pair
```

H1：

```text
允许 2 个 special pair
```

核心条件：

```text
pair_count >= 8
swapped coverage >=75%
special_base_values.size() <= 2
```

用户不是去改算法，而是验证：

```text
应该触发的 2-special 能触发
不该触发的 negative 不触发
Formal PASS
hidden 有收益
public 不退化
scale 正常
```

---

# 十七、H1 开发侧参考结果

## 2-special probe

```text
PAIR WIDTH STABLE H1 DELTA

8    1     38    28   -10
8    2     60    46   -14
8    4     104   89   -15
8    8     192   139  -53
8    12    280   202  -78

16   1     81    49   -32
16   2     123   77   -46
16   4     207   134  -73
16   8     375   245  -130
16   12    543   388  -155
```

10/10 EQY PASS。

---

## H1 dedicated regression

```text
h1_n1_three_special        104 → 104   no trigger   PASS
h1_n2_small_two_special     48 → 48    no trigger   PASS
h1_n3_width_mismatch       148 → 148   no trigger   PASS
h1_n4_different_control    120 → 120   no trigger   PASS
h1_n5_partial_decode       103 → 103   no trigger   PASS
h1_n6_ctrlwidth1             8 → 8     no trigger   PASS

h1_p1_threshold            104 → 84    trigger      PASS
h1_p2_swapbit2             192 → 168   trigger      PASS
h1_p3_shuffle               60 → 46    trigger      PASS
h1_p4_large                797 → 355   trigger      PASS
h1_p5_zero_special          56 → 56    trigger      PASS
h1_p6_one_special           43 → 43    trigger      PASS
```

注意：

```text
公开 test1~test4 预期不会因为 H1 改变。
```

所以 public 没额外下降：

```text
不是失败。
```

---

# 十八、当前接下来必须执行的顺序

## Step 1：先锁定 exact commit

先执行：

```bash
cd /home/fpga/fpga/pmux-opt

git status --short
git fetch --all --prune
git log -5 --oneline
git branch -vv
git rev-parse --verify 0c0ee40^{commit}
```

如果：

```text
0c0ee40 不存在
```

则：

```text
停止
让算法负责人确认是否已经 push
```

---

## Step 2：fresh plugin

建议工作目录：

```text
/tmp/pmux-h1-0c0ee40
```

流程：

```text
git show exact commit source
→ source SHA256
→ cmp
→ matching yosys-config
→ fresh pmux_opt.so
→ plugin SHA256
→ baseline Yosys 无内建 pmux_opt
→ -m fresh plugin
→ help pmux_opt
```

---

## Step 3：主评测流程 P public test1~4

每个 case 记录：

```text
baseline Total
optimized Total

baseline Comb
optimized Comb

baseline DFF
optimized DFF

candidate/rewrite count
pairwise EQY
elapsed
RSS
```

P 参考 aggregate：

```text
Total 611 → 525
Comb 358 → 277
DFF 249 → 244
```

H1 不应改变 public QoR。

---

## Step 4：P pairwise Formal

保存：

```text
baseline_generic.il
optimized_generic.il
```

Formal：

```text
baseline generic
vs
optimized generic
```

test1~4 必须全部 PASS。

---

## Step 5：H1 专项

重点验证：

```text
2-special positive
3-special negative
small 2-special negative
width mismatch
different control
partial decode
ctrlwidth=1
shuffle
large
zero special
one special
```

检查：

```text
candidate
rewrite
resource
Formal
```

---

## Step 6：R1

流程：

```text
proc
→ pmux_opt
→ opt_clean
```

至少覆盖：

```text
public test1~4
H1 positive
H1 negative
```

检查：

```text
crash
assert
Formal
candidate
明显资源退化
```

R1 不参与 P aggregate。

---

## Step 7：Negative EQY

必须验证：

```text
正确设计 → PASS
故意破坏设计 → FAIL
```

历史负例：

```text
rc=2
```

如果故意破坏都 PASS：

```text
Formal 环境不可信。
```

---

## Step 8：Hidden-style

不要只重复开发侧正例。

重点：

```text
selector reorder
swap bit 变化
width 变化
pair_count 变化
2 special
3 special
partial decode
control mismatch
width mismatch
extra user
signed comparator
unknown/X
direct output
small unprofitable
mixed logic
```

目的：

```text
主动找边界和失败
不是替算法证明它一定没问题
```

---

## Step 9：1000-node

继续：

```text
mixed_1000
pattern_a_1000
negative_1000
```

历史参考：

```text
mixed_1000:
~995 nodes
2341 → 1675

pattern_a_1000:
~996 nodes
4150 → 2656

negative_1000:
1000 nodes
2600 → 2600
```

性能限制：

```text
runtime <=60s
RSS <=2GB
```

---

# 十九、H1 PASS 条件

至少满足：

```text
1. exact commit/source/plugin chain 完整
2. P public test1~4 可运行
3. P pairwise EQY 全 PASS
4. P 资源与开发参考一致，或差异可解释
5. R1 无 crash / 真非等价
6. H1 2-special 正例能触发
7. negative 不误触发
8. Negative EQY 自检正常
9. 1000-node PASS
10. 无明显 time/RSS 回归
```

---

# 二十、出现差异时固定排查顺序

不要先修改算法。

按：

```text
1. exact commit
2. source SHA256
3. plugin SHA256
4. Yosys -V
5. yosys-config
6. help before/after plugin
7. RTL SHA256
8. 实际 pass 顺序
9. mapping family
10. generic netlist
11. EQY log
```

---

# 二十一、最后交付报告建议

标题：

```text
H1 / 0c0ee40 独立验证报告
```

结构：

```text
1. 环境

2. exact commit / source / plugin 证据链

3. 主流程 P
   public test1~4
   Total / Comb / DFF
   trigger
   time/RSS

4. P Formal
   baseline generic vs optimized generic
   每个 case PASS/FAIL

5. R1
   public
   positive
   negative
   Formal
   crash/resource regression

6. Hidden-style

7. Negative EQY

8. 1000-node

9. 与开发侧差异说明

10. 最终结论
    PASS / CONDITIONAL / FAIL
```

---

# 二十二、工作方式要求

后续对话应：

```text
先结论
→ 当前验证阶段
→ 一次给一组命令
→ 说明命令作用
→ 等用户贴真实输出后再继续
```

不要：

```text
一次发几十条未来命令
没有日志就猜
EQY FAIL 就直接说算法错
主动修改 pmux_opt.cc
```

最重要的一句话：

> 你不是第二个算法开发者。你的价值是：用独立环境与操作链证明 exact commit 在同一主评测流程 P 下真的能复现 QoR 和 Formal，并主动寻找 hidden / negative / scale 边界。

---

# 二十三、当前接棒点

现在不要继续讨论 Pattern D，它已经完成定向验证。

**当前唯一主任务：**

```text
开始 H1 / 0c0ee40 独立验证。
```

下一条应该先让用户执行：

```bash
cd /home/fpga/fpga/pmux-opt

git status --short
git fetch --all --prune
git log -5 --oneline
git branch -vv
git rev-parse --verify 0c0ee40^{commit}
```

拿到真实输出后：

```text
若 commit 存在
→ 进入 fresh plugin 构建

若不存在
→ 停止，让算法负责人确认是否 push
```